import styled from "styled-components"

import { Cover } from "../globalStyles"

export const LoadingCover = styled(Cover)`
  width: 100%;
  height: 100%;
`

export const LoadingIcon = styled.img`
  width: 6rem;
  height: 6rem;
  margin: auto;
`
