import styled from "styled-components"

import { VerticalButtonsContainer } from "../globalStyles"

export const ButtonsContainer = styled(VerticalButtonsContainer)`
  left: 2rem;
  box-shadow: 0 0 0.5rem rgba(0, 0, 0, 0.25);
`
