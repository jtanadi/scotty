import styled from "styled-components"

const ViewContainer = styled.div`
  width: 100%;
  padding: 4.5rem 0 2rem 0;
  display: flex;
  background-color: lightgray;
`

const DocumentContainer = styled.div`
  margin: auto;
`

export { ViewContainer, DocumentContainer }
