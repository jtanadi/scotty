/*
For temp reference; turn into TS type or interface later
rooms = {
  id: {
    users: number
    pdfUrl: string,
    currentPage: number
  }
}

users = {
  userID: roomID
}
*/

const rooms = {}
const users = {}

module.exports = { rooms, users }
